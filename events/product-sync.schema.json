{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["product_id", "store", "status"],
  "properties": {
    "product_id": { "type": "string" },
    "store":      { "type": "string", "enum": ["AU", "GB", "NZ", "US"] },
    "status":     { "type": "string", "enum": ["active", "archived"] }
  },
  "additionalProperties": false
}